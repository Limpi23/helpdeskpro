# Variables de entorno para Railway
# Estas se configuran en el dashboard de Railway

# Base de datos MySQL (Railway las genera automáticamente)
DB_HOST=${MYSQLHOST}
DB_PORT=${MYSQLPORT}
DB_USERNAME=${MYSQLUSER}
DB_PASSWORD=${MYSQLPASSWORD}
DB_DATABASE=${MYSQLDATABASE}

# JWT
JWT_SECRET=tu_jwt_secret_super_seguro_aqui

# Puerto (Railway lo maneja automáticamente)
PORT=${PORT}

# Entorno
NODE_ENV=production

# Google OAuth (opcional - para más adelante)
GOOGLE_CLIENT_ID=tu_google_client_id
GOOGLE_CLIENT_SECRET=tu_google_client_secret

# CORS Origins (tu dominio de frontend)
FRONTEND_URL=https://tu-frontend.railway.app 